// Site-wide settings. Edit this file, not the HTML pages.
window.SITE = {
  name: "The Eclectic Boon & Bane",
  contactEmail: "hello@example.com", // TODO: replace with the real address
  etsyUrl: "",                       // TODO: paste Etsy shop URL once it exists
  instagramUrl: "",                  // optional

  // Newsletter ("Join the Guild"). Paste the form-post URL from an email service
  // (Buttondown, Mailchimp, ConvertKit...) to turn the form on. While empty, the
  // form politely says the Guild isn't open yet and collects nothing.
  newsletterAction: "",
  newsletterEmailField: "email"      // the field name the email service expects
};
